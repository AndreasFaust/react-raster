import { NextPage } from "next";
import { Grid } from "../dist";

interface Props {}

const Startseote: NextPage<Props> = (props) => {
  return (
    <Grid>
      <h1>Hallo!</h1>
    </Grid>
  );
};

export default Startseote;
